import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.citasliterariasapp',
  appName: 'citasliterariasapp',
  webDir: 'www'
};

export default config;
